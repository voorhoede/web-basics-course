mapboxgl.accessToken = "";

/**
 * Get element of the map and assign it to a mapElement variable
 */

/**
 * The value of the container should be the element you retrieved above
 */
const mapConfig = {
  container: null,
  style: "mapbox://styles/mapbox/dark-v11",
  center: [5.5, 52.1],
  zoom: 6,
};

/**
 * Initialize the Mapbox map
 * Hint: https://docs.mapbox.com/mapbox-gl-js/example/simple-map/
 */
